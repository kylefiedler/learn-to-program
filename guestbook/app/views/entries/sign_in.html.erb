<h1>Hello <%= @name %></h1>

<%= form_tag action: 'sign_in' do %>
  <%= text_field_tag 'visitor_name', @name, placeholder: 'Enter your name' %>
  <%= submit_tag 'Sign in' %>
<% end %>

<h2>Previous Visitors</h2>
<ul>
  <% @entries.each do |entry| %>
    <li><%= entry.name %></li>
  <% end %>
</ul>
